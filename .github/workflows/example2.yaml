name: Hello World Workflow

on:
  push:
    branches:
       - '*'

jobs:
  hello-world:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2

      - name: Print Hello World
        run: exit 1

      - name: Print salam eleykim
        run: echo "salam eleykim"